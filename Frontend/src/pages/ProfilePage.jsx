import ProfilePageComponent from '../components/Profile/ProfilePage';

// This is a forwarding component that re-exports the ProfilePage component
const ProfilePage = () => {
    return <ProfilePageComponent />;
};

export default ProfilePage; 